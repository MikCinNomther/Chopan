<Page x:Class="Chopan.Pages.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Chopan.Pages"
      mc:Ignorable="d" 
      SizeChanged="Page_SizeChanged"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Grid Background="#D1FFFFFF" x:Name="Back">
        <Grid Margin="20">
            <TextBlock Text="登录" VerticalAlignment="Top" HorizontalAlignment="Left" FontSize="25" FontWeight="Bold"/>
            <GroupBox Header="" Margin="0,35,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="70"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="1">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="20"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="用户名：" FontSize="25"/>
                        <TextBox x:Name="Username" FontSize="24" Grid.Column="1"/>
                        <TextBlock Grid.Row="1" Text="密码：" FontSize="25"/>
                        <PasswordBox x:Name="Password" FontSize="24" Grid.Row="1" Grid.Column="1"/>
                    </Grid>
                    <Button Click="Button_Click" Grid.Row="2" HorizontalAlignment="Right" VerticalAlignment="Top" Height="60" Width="150" Margin="0,20,60,0" FontSize="30" Content="登录" Foreground="White" Background="LightGray"/>
                    <TextBlock Text="安全 - 稳定 - 简单" Foreground="#5F000010" Grid.Row="2" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>
                </Grid>
            </GroupBox>
        </Grid>
    </Grid>
</Page>
